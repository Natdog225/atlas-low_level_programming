#include "main.h"
/**
 * _isalpha - checks for the abcs
 * @c: the int to check
 * Return: 1 if it is a letter, 0 if not
 */
int _isalpha(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
