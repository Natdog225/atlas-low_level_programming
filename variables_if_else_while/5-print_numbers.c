#include <stdio.h>
/**
 * main - function and main entry point
 *
 * Return: 0 always (successful)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)

	{
		printf("%d ", i);
	}
	printf("%d ", i);
	return (0);
}
