#include <stdio.h>
/**
 * main - function and entry point
 * Return: Always 0 (success)
 */
int main(void)
{
for (char c = 'a'; c <= 'z'; c++)
}
putchar(' ');
for (char c = 'A'; c <= 'Z'; c++)
{
putchar(c);
}
putchar('\n');
return (0);
}
