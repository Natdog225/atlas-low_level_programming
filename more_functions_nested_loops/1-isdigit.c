#include "main.h"
/**
 * Description: Tells you if if the input is a digit
 *
 * isdigit - tells if its a digit
 *
 * @c: is the int
 *
 * Return: Always 0
 */
int _isdigit(int c)
{
	return (c >= 48 && c <= 57);
}
